# Django Settings
SECRET_KEY=your-secret-key-here-generate-with-django-get-secret-key
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1,*

# Database Configuration
# For SQLite (default):
DATABASE_ENGINE=django.db.backends.sqlite3
DATABASE_NAME=db.sqlite3

# For PostgreSQL (recommended for production):
# DATABASE_ENGINE=django.db.backends.postgresql
# DATABASE_NAME=trainee_tracker
# DATABASE_USER=postgres
# DATABASE_PASSWORD=your-db-password
# DATABASE_HOST=localhost
# DATABASE_PORT=5432

# Application Settings
TIME_ZONE=America/Chicago
LANGUAGE_CODE=en-us

# Security Settings (Production Only)
# Uncomment these for HTTPS deployment:
# SECURE_SSL_REDIRECT=True
# SESSION_COOKIE_SECURE=True
# CSRF_COOKIE_SECURE=True
# SECURE_HSTS_SECONDS=31536000
# SECURE_HSTS_INCLUDE_SUBDOMAINS=True
# SECURE_HSTS_PRELOAD=True

# CSRF Trusted Origins (for network deployment)
# CSRF_TRUSTED_ORIGINS=http://192.168.1.100:8000,http://your-server-ip:8000
